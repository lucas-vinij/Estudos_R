# Algoritmo Ler do teclado

# Informar que usuario deve digitar um numero
# Ler o numero do teclado e atribuir a uma variavel
# Escrever na tela o numero lido

cat('Digite um numero inteiro: ')
n <- scan(stdin(), n=1, what= integer())
cat('O numero lido foi',n)

